#include<iostream>
#include<cstdio>
#include<cstring>
using namespace std;

const int maxn = 500005, maxk = 2005;
int n,k,cnt,tt;
char c;

int main(){
	freopen("omega.in","r",stdin);
	freopen("omega.out","w",stdout);
	ios::sync_with_stdio(false);
	cin>>n>>k;
	if(!k) {cout<<n<<endl; return 0;}
	cout<<1<<endl;
	fclose(stdin); fclose(stdout);
	return 0;
}
